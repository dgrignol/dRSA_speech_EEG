INPUT FROM PIPELINE: cfg.modelVec = [1 3 4 5]; %what models do I want to test cfg.modelRegressout = []; %what models do I want to regress out but not test cfg.condVec = 1:4; cfg.ROINumber = [3]; %which ROI? occipital, 'frontal', 'parietal' 'wholeBrain' ? cfg.saving = 1; %saving activate (1) or not (0) %for dRSA opt.SubSampleDurSec = 5; % in sec opt.nSubSamples = 15; opt.nIter = 1000; opt.dRSA.corrMethod = 'PCR'; %'corr' 'PCR'; %.5; %we average at the end for 1.5s % For PCR opt.dRSA.Additional_PCA = 1; %Should we do a second PCA to reduce the size of the models before the final PCA or not? opt.dRSA.Normalize = 'Rescale'; %'Rescale'; %'Standardize' opt.dRSA.PCRMethod = 'MinCompPCR'; %'CumulativeVar' %'ExplainedVar' %'FixedComp' %'MinCompPCR';
opt.percPCA = 0.85; cfg.script2run = 'MD_run_dRSA_v4'; %4 th version separately for ROIs - cluster! %redo 4 SubVec = [1 2 4:22]; cfg.cluster =1; cluster_shell_isub (SubVec, cfg, opt);